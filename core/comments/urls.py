from django.urls import path
# from comments.views import (
#     CommentCreateView,
# )

app_name = "comments"

urlpatterns = [
    # path("comments/<int:pk>", CommentCreateView.as_view(), name="comment-list"),
]
